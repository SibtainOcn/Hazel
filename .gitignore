*.iml
.gradle
/local.properties
local.properties

# IDE — never commit
.idea/
.kotlin/

.DS_Store
/build
/captures
.externalNativeBuild
.cxx
/app/build
/app/release
/release/
DISCORD-PLAN.md
main.tf

# APK / Bundle artifacts
*.apk
*.ap_
*.aab
*.dm

# Signing — never commit
signing.properties
*.jks
*.keystore
/signing/

# Internal
ISSUES.md
build_output.txt

# Terraform — secrets & generated files must NOT be committed
discord_build/terraform.tfvars
discord_build/.terraform/
discord_build/.terraform.lock.hcl
discord_build/terraform.tfstate
discord_build/terraform.tfstate.backup
# main.tf is safe to commit (no secrets inside)
