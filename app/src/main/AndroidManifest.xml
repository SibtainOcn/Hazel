<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!--
        Lets the app ask to be exempt from battery optimisation. A download is a long
        network job, and without the exemption the system suspends it shortly after the
        app leaves the foreground. The user is only ever asked through the system's own
        screen; nothing is granted silently.
    -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!--
         What the app is allowed to see of the rest of the device. From Android 11 an app
         cannot resolve an intent at another package without saying so, and the sheet offers
         to open a link where it belongs: in the site's own app when it is installed, in the
         browser otherwise.
    -->
    <queries>
        <package android:name="com.google.android.youtube" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:name=".HazelApp"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true"
        android:localeConfig="@xml/locales_config"
        android:theme="@style/Theme.Hazel">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="locale|layoutDirection"
            android:theme="@style/Theme.Hazel.Splash">

            <!-- Normal launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Translucent share overlay activity.
            Catches shared URLs from external applications and renders the format selection
            bottom sheet directly over the host app without switching windows or screen stacks.
        -->
        <activity
            android:name=".ui.share.ShareOverlayActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:launchMode="singleInstance"
            android:theme="@style/Theme.Hazel.Overlay"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="locale|layoutDirection|orientation|screenSize|smallestScreenSize|screenLayout">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <!--
            Instant share target.
            Appears in the Android system share sheet as "Instant". Catches shared URLs and
            downloads at the user's saved quick settings via a minimal overlay confirmation.
        -->
        <activity-alias
            android:name=".DirectShareActivity"
            android:targetActivity=".ui.share.ShareOverlayActivity"
            android:exported="true"
            android:label="@string/direct_share_label"
            android:icon="@mipmap/ic_launcher">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity-alias>

        <!--
             Holds the process open while a download runs. It does no work of its own: the
             queue lives in the app, and this only stops the system stopping it.
        -->
        <service
            android:name=".download.DownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!--
             Carries the notification's Pause, Resume and Cancel buttons to the download
             they belong to. Not exported: nothing outside the app has any business
             stopping a download the user started.
        -->
        <receiver
            android:name=".download.DownloadActionReceiver"
            android:exported="false" />

        <!-- Sign-in browser used to collect cookies for gated media -->
        <activity
            android:name=".ui.screens.cookies.CookieWebViewActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:theme="@style/Theme.Hazel" />

        <!-- FileProvider for opening downloaded files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
